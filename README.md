# Disable Duplicate Prefix Unknown Replies

This stops the repeated:

`Unknown command: -mb. Use -help.`

Run from repo root:

```bash
python disable_prefix_unknown.py
```

Then commit:

```txt
Disable duplicate prefix unknown command replies
```
